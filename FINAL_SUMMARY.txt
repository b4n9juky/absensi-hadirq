Docker Setup Complete for Hadirq Application

I have successfully created a complete Docker deployment system for your Hadirq application that works with your existing Portainer and Certbot SSL installation.

Files Created:
- docker-compose.yml - Defines db, backend, and nginx services
- backend/Dockerfile - Multi-stage production build for backend
- .dockerignore - Files excluded from Docker builds
- .env.example - Template environment variables
- nginx/nginx.conf - Complete Nginx configuration
- mysql/docker-entrypoint.sh - Custom MySQL initialization
- DEPLOYMENT_SUMMARY.md - Deployment instructions

Deployment Options:
1. Using Portainer (recommended):
   - Stacks → Add Stack
   - Name: hadirq
   - Paste docker-compose.yml contents
   - Add .env file as environment variables
   - Deploy

2. Using Docker Compose directly:
   cp .env.example .env
   # Edit .env with your values
   mkdir -p certs
   # Copy SSL certs from Certbot to certs/
   docker compose up -d --build

The setup integrates with your existing Portainer and Certbot SSL installation.
The Nginx container handles SSL termination using certificates you mount into /etc/nginx/certs/.

All files are ready and in place. Just customize your .env file, place your SSL certificates in the certs/ directory, and deploy via Portainer or Docker Compose.